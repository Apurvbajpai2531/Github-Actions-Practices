name: Daily Schedule

on:
  schedule:
    - cron: "0 0 * * *"

jobs:
  scheduled-job:
    runs-on: ubuntu-latest

    steps:
      - name: Run Scheduled Workflow
        run: |
          echo "Scheduled workflow executed!"
          date